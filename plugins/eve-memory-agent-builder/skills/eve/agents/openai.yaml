interface:
  display_name: "Eve"
  short_description: "Create a disk-efficient local Eve memory and Wiki agent"
  default_prompt: "Use $eve to create and validate a disk-efficient local Eve agent with shared pnpm dependencies, Codex-authenticated model access, durable embedded memory, and a read-only LLM Wiki."

policy:
  allow_implicit_invocation: true
