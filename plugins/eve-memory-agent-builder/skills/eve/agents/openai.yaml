interface:
  display_name: "Eve"
  short_description: "Create a local Eve agent or specialist team"
  default_prompt: "Use $eve to create and validate a local Eve agent or coordinator-led specialist team with safe memory boundaries."

policy:
  allow_implicit_invocation: true
