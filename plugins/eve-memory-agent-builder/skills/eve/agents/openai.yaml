interface:
  display_name: "Eve"
  short_description: "Create a complete Eve memory and Wiki agent"
  default_prompt: "Use $eve to create and validate a complete Eve agent with durable memory and a read-only LLM Wiki."

policy:
  allow_implicit_invocation: true
